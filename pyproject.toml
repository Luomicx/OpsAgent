[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "ops-agent"
version = "0.1.0"
description = "基于 SSH 的智能运维 Agent：插件化内核 + 分层权限控制 + 可审计会话事件流"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "asyncssh>=2.14",
    "pydantic>=2.7",
    "pyyaml>=6.0",
    "httpx>=0.27",
]

[project.optional-dependencies]
dev = [
    "pytest>=8.0",
    "pytest-asyncio>=0.23",
    "ruff>=0.5",
    "mypy>=1.10",
]

[project.scripts]
ops-agent = "ops_agent.cli:main"
# 桌面端 sidecar：从 stdin 读 NDJSON 请求，往 stdout 写响应与事件
ops-agent-bridge = "ops_agent.bridge.sidecar:main"

[tool.setuptools.packages.find]
where = ["src"]

[tool.pytest.ini_options]
asyncio_mode = "auto"
asyncio_default_fixture_loop_scope = "function"
# 只收 Python 测试；desktop/ 是独立的 Tauri 前端工程，不参与 pytest 采集
testpaths = ["tests"]

[tool.ruff]
line-length = 100
target-version = "py311"
src = ["src", "tests"]
# desktop/ 是 Tauri 前端（node_modules 由 npm 管理），不参与 Python lint
exclude = ["desktop", ".venv"]

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B", "SIM", "RUF", "BLE"]
ignore = ["RUF001", "RUF002", "RUF003"]

[tool.mypy]
python_version = "3.11"
warn_unused_configs = true
disallow_untyped_defs = true
disallow_incomplete_defs = true
check_untyped_defs = true
no_implicit_optional = true
warn_redundant_casts = true
ignore_missing_imports = true
